apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - name: cert-manager
            cluster: in-cluster            
            path: platform/cert-manager
            namespace: cert-manager
            type: platform
            owner: platform-team
            syncwave: "0"
          - name: opentelemetry-operator
            cluster: in-cluster
            path: platform/opentelemetry-operator
            namespace: monitoring
            type: platform
            owner: platform-team
            syncwave: "0"
          - name: collector
            path: platform/collector
            namespace: monitoring
            cluster: in-cluster
            type: platform
            owner: platform-team
            syncwave: "1"
          
          
  template:
    metadata:
      name: "{{.name}}"
      namespace: argocd
      labels:
        gitops.taz.dev/owner: "{{.owner}}"
        gitops.taz.dev/type: "{{.type}}"
        gitops.taz.dev/cluster: "{{.cluster}}"
        gitops.taz.dev/namespace: "{{.namespace}}"
      annotations:
        argocd.argoproj.io/sync-wave: "{{.syncwave}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/Tazmainiandevil/gitops.tracing.git
        path: "{{.path}}"
        targetRevision: main
      destination:
        namespace: "{{.namespace}}"
        name: "{{.cluster}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
